const value = 3;
console.log(value + 4 + value);

// 343 was logged because value was a character. Therefor using the "+" sign concatenated 3, 4 and 3. Resulting in 343. Changing data type of value fixed the problem as value will now be processed as an integer instead. 